#include <iostream>
using namespace std;

void PrintArray(int arr[], int size)
{
    
    for (size_t i = 0; i < size; i++)
    {
        cout << arr[i] << endl;
    }
}

int main()
{
    /*
    int nums[15];
    fill_n(nums, 15, -1);

    // for (size_t i = 0; i < 100; i++)
    // {
    //     cout<<nums[i]<<endl;
    // }
    PrintArray(nums, sizeof(nums) / sizeof(int));
    // if some elements are initialsed then remiang are set to 0
    */

    /*
       char ch[5] = {'a','b','c'};
       cout<<ch[2];
    */

    // bool firstBool[9];


    

    return 0;
}